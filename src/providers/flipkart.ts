// Placeholder for optional Flipkart provider via affiliate APIs.
import { NormalizedProduct } from '../config/types.js';

export async function searchFlipkart(_query: string): Promise<NormalizedProduct[]> {
  return [];
}
